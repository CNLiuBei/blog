---
title: Hexo 博客部署到 Cloudflare Pages 完整教程（小白专用）
date: 2025-11-16 11:05:00
categories: 
  - 教程
tags:
  - Hexo
  - Cloudflare
  - 部署
  - 教程
description: 手把手教你如何将 Hexo 博客免费部署到 Cloudflare Pages，无需任何服务器知识！包含两种部署方法、常见问题解决和进阶配置。
---

> 🎯 本教程将手把手教你如何将 Hexo 博客免费部署到 Cloudflare Pages，无需任何服务器知识！

<!-- more -->

## 📋 目录

1. [准备工作](#准备工作)
2. [什么是 Cloudflare Pages](#什么是-cloudflare-pages)
3. [部署方式选择](#部署方式选择)
4. [方法一：通过 GitHub 自动部署（推荐）](#方法一通过-github-自动部署推荐)
5. [方法二：通过命令行直接部署](#方法二通过命令行直接部署)
6. [绑定自定义域名](#绑定自定义域名)
7. [常见问题解决](#常见问题解决)
8. [进阶配置](#进阶配置)

---

## 准备工作

### ✅ 需要准备的东西

1. **Hexo 博客项目**
   - 确保你本地已经有一个 Hexo 博客
   - 可以正常运行 `hexo server` 预览

2. **GitHub 账号**（方法一需要）
   - 注册地址：https://github.com/
   - 完全免费

3. **Cloudflare 账号**
   - 注册地址：https://dash.cloudflare.com/sign-up
   - 完全免费
   - 支持中文界面

4. **Git 工具**
   - Mac 用户：系统自带，无需安装
   - Windows 用户：下载安装 https://git-scm.com/

### 📦 确认你的 Hexo 博客正常运行

在博客目录下运行：

```bash
# 进入你的博客目录
cd /Users/liubei/Desktop/blog

# 清理缓存
hexo clean

# 生成静态文件
hexo generate

# 本地预览（确保能正常显示）
hexo server
```

如果能在浏览器访问 `http://localhost:4000` 看到你的博客，说明一切正常！

---

## 什么是 Cloudflare Pages

### 🌟 Cloudflare Pages 的优势

- ✅ **完全免费** - 无限流量、无限构建次数
- ⚡ **超快速度** - 全球 CDN 加速，国内访问速度优秀
- 🔒 **免费 HTTPS** - 自动配置 SSL 证书
- 🚀 **自动部署** - 推送代码即自动更新网站
- 🎯 **零配置** - 不需要服务器知识
- 📊 **访问统计** - 内置网站分析功能
- 🌐 **自定义域名** - 支持绑定你自己的域名

### 📊 与其他平台对比

| 特性 | Cloudflare Pages | Vercel | Netlify | GitHub Pages |
|-----|------------------|--------|---------|--------------|
| 国内访问速度 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| 免费流量 | 无限 | 100GB/月 | 100GB/月 | 无限 |
| 构建次数 | 无限 | 100次/天 | 300分钟/月 | 无限 |
| HTTPS | ✅ | ✅ | ✅ | ✅ |
| 自定义域名 | ✅ | ✅ | ✅ | ✅ |

---

## 部署方式选择

### 🎯 两种部署方式

#### 方法一：GitHub 自动部署（⭐ 推荐新手）

**优点**：
- ✅ 自动化，推送代码即部署
- ✅ 有版本控制，可以回滚
- ✅ 团队协作方便
- ✅ 配置一次，终身使用

**缺点**：
- ❌ 需要学习基本的 Git 操作
- ❌ 首次配置稍微复杂

**适合人群**：想要长期维护博客的用户

---

#### 方法二：命令行直接部署

**优点**：
- ✅ 部署快速，一条命令搞定
- ✅ 不需要 GitHub
- ✅ 本地完全控制

**缺点**：
- ❌ 每次更新都要手动部署
- ❌ 没有版本控制
- ❌ 无法自动化

**适合人群**：临时部署或快速测试

---

## 方法一：通过 GitHub 自动部署（推荐）

### 📝 第一步：注册 GitHub 账号

1. 访问 https://github.com/
2. 点击右上角 **Sign up**
3. 填写邮箱、密码、用户名
4. 验证邮箱

### 📦 第二步：创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角 ➕ → **New repository**
3. 填写仓库信息：
   ```
   Repository name: my-blog          # 仓库名称（随便起）
   Description: 我的个人博客          # 描述（可选）
   Public / Private: Public          # 选择 Public（公开）
   ✅ Add a README file               # 不勾选
   ```
4. 点击 **Create repository**

### 💻 第三步：将博客推送到 GitHub

在你的博客目录下运行以下命令：

```bash
# 1. 初始化 Git（如果还没有初始化）
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit - 我的博客"

# 4. 添加远程仓库（替换成你的 GitHub 用户名）
git remote add origin https://github.com/你的用户名/my-blog.git

# 5. 推送到 GitHub
git branch -M main
git push -u origin main
```

**💡 提示**：
- 如果提示输入用户名和密码，输入你的 GitHub 账号信息
- 如果提示需要 Token，参考：https://docs.github.com/zh/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token

### 🎯 第四步：连接 Cloudflare Pages

#### 1. 注册 Cloudflare 账号

- 访问：https://dash.cloudflare.com/sign-up
- 使用邮箱注册（建议用 Gmail 或 QQ 邮箱）
- 验证邮箱

#### 2. 创建 Pages 项目

1. 登录 Cloudflare Dashboard
2. 在左侧菜单找到 **Workers 和 Pages** 或直接访问：https://dash.cloudflare.com/pages
3. 点击 **创建应用程序** → **Pages** → **连接到 Git**
4. 选择 **GitHub**
5. 授权 Cloudflare 访问你的 GitHub

#### 3. 配置部署

选择你刚创建的仓库 `my-blog`，然后配置：

```yaml
项目名称: my-blog                    # 项目名称（会成为你的网址）
生产分支: main                       # 主分支名称

构建设置:
  框架预设: Hexo                     # 选择 Hexo
  构建命令: hexo generate            # 自动填充
  构建输出目录: public               # 自动填充
```

**环境变量**（可选，如果构建失败再添加）：
```
变量名: NODE_VERSION
值: 18.17.0
```

#### 4. 开始部署

点击 **保存并部署**，等待 2-5 分钟。

你会看到部署进度：
```
⏳ 克隆仓库...
⏳ 安装依赖...
⏳ 运行构建命令...
⏳ 上传资源...
✅ 部署成功！
```

### 🎉 第五步：访问你的网站

部署成功后，Cloudflare 会给你一个网址：

```
https://my-blog-xxx.pages.dev
```

**恭喜！🎊 你的博客已经上线了！**

### 🔄 第六步：以后如何更新博客

以后每次写完新文章，只需要：

```bash
# 1. 添加新文章到 Git
git add .

# 2. 提交
git commit -m "新增文章：我的第一篇博客"

# 3. 推送到 GitHub
git push
```

推送后，Cloudflare Pages 会**自动**构建和部署，2-3 分钟后就能看到更新！

---

## 方法二：通过命令行直接部署

### 📦 第一步：安装 Wrangler CLI

Wrangler 是 Cloudflare 的官方命令行工具。

```bash
# 全局安装
npm install -g wrangler

# 验证安装
wrangler --version
```

### 🔐 第二步：登录 Cloudflare

```bash
# 登录
wrangler login
```

这会打开浏览器，登录你的 Cloudflare 账号并授权。

### 📤 第三步：部署博客

```bash
# 1. 进入博客目录
cd /Users/liubei/Desktop/blog

# 2. 清理并生成静态文件
hexo clean
hexo generate

# 3. 部署到 Cloudflare Pages
npx wrangler pages deploy public --project-name=my-blog
```

**首次部署会提示**：
```
? Create a new project? (Y/n)
```
输入 `Y` 并回车。

### ✅ 部署完成

你会看到：

```
✨ Success! Uploaded 245 files (3.2 sec)

✨ Deployment complete! Take a peek over at https://xxxxxxxx.my-blog.pages.dev
```

访问这个网址就能看到你的博客了！

### 🔄 以后如何更新

每次更新博客：

```bash
# 1. 清理并重新生成
hexo clean && hexo generate

# 2. 部署
npx wrangler pages deploy public --project-name=my-blog
```

---

## 绑定自定义域名

### 📝 前提条件

- 你已经有一个域名（如 `example.com`）
- 域名 DNS 托管在 Cloudflare 或可以修改 DNS

### 🔧 配置步骤

#### 方法 A：域名已在 Cloudflare

1. 进入 Cloudflare Dashboard
2. 选择你的 Pages 项目
3. 点击 **自定义域** → **设置自定义域**
4. 输入你的域名（如 `blog.example.com`）
5. 点击 **继续**
6. Cloudflare 会自动添加 DNS 记录

**等待 1-2 分钟**，访问你的自定义域名即可！

#### 方法 B：域名不在 Cloudflare

1. 在 Pages 项目中添加自定义域名
2. Cloudflare 会给你一个 CNAME 记录：
   ```
   blog.example.com → my-blog-xxx.pages.dev
   ```
3. 去你的域名注册商添加这条 CNAME 记录
4. 等待 DNS 生效（通常 10 分钟到 1 小时）

### 🔒 HTTPS 证书

Cloudflare 会自动为你的自定义域名配置免费 SSL 证书，无需任何操作！

---

## 常见问题解决

### ❓ 问题 1：部署失败，提示 "Build failed"

**原因**：Node.js 版本不匹配

**解决方法**：

在 Cloudflare Pages 项目设置中添加环境变量：

```
变量名: NODE_VERSION
值: 18.17.0
```

然后重新部署。

---

### ❓ 问题 2：推送到 GitHub 失败

**错误提示**：
```
fatal: unable to access 'https://github.com/...': 
Failed to connect to github.com port 443
```

**解决方法**：

```bash
# 检查 Git 配置
git config --global --list

# 重新配置用户信息
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"

# 重试推送
git push -u origin main
```

---

### ❓ 问题 3：网站显示 404

**原因**：
- 构建输出目录配置错误
- 没有生成 index.html

**解决方法**：

确保 Cloudflare Pages 的构建配置：
```
构建命令: hexo generate
构建输出目录: public
```

---

### ❓ 问题 4：CSS/JS 加载失败

**原因**：资源路径错误

**解决方法**：

修改 `_config.yml`：

```yaml
# 如果使用自定义域名
url: https://blog.example.com
root: /

# 如果使用 pages.dev 域名
url: https://my-blog-xxx.pages.dev
root: /
```

然后重新部署。

---

### ❓ 问题 5：每次都要输入 GitHub 密码

**解决方法**：

使用 SSH 密钥：

```bash
# 1. 生成 SSH 密钥
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# 2. 复制公钥
cat ~/.ssh/id_rsa.pub

# 3. 添加到 GitHub
# 访问 https://github.com/settings/keys
# 点击 "New SSH key"，粘贴公钥

# 4. 修改远程地址为 SSH
git remote set-url origin git@github.com:你的用户名/my-blog.git
```

---

### ❓ 问题 6：部署后文章不显示

**检查清单**：

1. 确认文章在 `source/_posts/` 目录下
2. 文章文件是 `.md` 格式
3. 文章有正确的 Front Matter：
   ```yaml
   ---
   title: 文章标题
   date: 2025-11-16 12:00:00
   ---
   ```
4. 本地运行 `hexo server` 能看到文章

---

## 进阶配置

### 🚀 启用预览部署

每次推送代码时，Cloudflare 会自动创建预览链接，方便测试。

在项目设置中：
```
构建 → 分支部署 → 启用
```

### 📊 启用 Web Analytics

免费的网站访问统计：

1. 进入 Cloudflare Dashboard
2. 选择 **Web Analytics**
3. 添加你的网站
4. 复制跟踪代码
5. 添加到 Hexo 主题的 `<head>` 标签中

### 🔒 启用访问控制

如果不想让所有人访问：

1. 进入 Pages 项目设置
2. 选择 **访问策略**
3. 添加 **Cloudflare Access** 规则

### ⚡ 配置自动清除缓存

在 `.github/workflows/deploy.yml` 中添加：

```yaml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install Dependencies
        run: npm install
        
      - name: Build
        run: npm run build
        
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: my-blog
          directory: public
```

---

## 🎓 学习资源

### 📚 官方文档

- **Cloudflare Pages**: https://developers.cloudflare.com/pages/
- **Hexo 文档**: https://hexo.io/zh-cn/docs/
- **Git 教程**: https://git-scm.com/book/zh/v2

### 🎥 视频教程

- 哔哩哔哩搜索 "Cloudflare Pages 部署"
- YouTube 搜索 "Cloudflare Pages Tutorial"

### 💬 社区支持

- Cloudflare 中文论坛：https://community.cloudflare.com/
- Hexo GitHub Discussions：https://github.com/hexojs/hexo/discussions

---

## 📋 快速命令参考

### Git 常用命令

```bash
# 查看状态
git status

# 添加所有修改
git add .

# 提交
git commit -m "更新内容"

# 推送
git push

# 查看日志
git log --oneline

# 回退到上一个版本
git reset --hard HEAD^
```

### Hexo 常用命令

```bash
# 新建文章
hexo new "文章标题"

# 清理缓存
hexo clean

# 生成静态文件
hexo generate
# 或简写
hexo g

# 本地预览
hexo server
# 或简写
hexo s

# 生成并启动服务器
hexo s -g
```

### Wrangler 常用命令

```bash
# 登录
wrangler login

# 部署
wrangler pages deploy public --project-name=my-blog

# 查看项目列表
wrangler pages project list

# 查看部署历史
wrangler pages deployment list --project-name=my-blog
```

---

## ✅ 部署检查清单

完成部署后，检查以下项目：

- [ ] 网站能正常访问
- [ ] 所有文章都显示正常
- [ ] 图片、CSS、JS 加载正常
- [ ] 主题样式正确
- [ ] 导航链接有效
- [ ] 评论系统工作（如果有）
- [ ] 搜索功能正常（如果有）
- [ ] 移动端显示正常
- [ ] HTTPS 证书有效
- [ ] 自定义域名解析（如果配置了）

---

## 🎉 恭喜完成！

现在你已经成功将 Hexo 博客部署到 Cloudflare Pages！

### 🌟 接下来可以做什么？

1. **写更多文章** - 专注于内容创作
2. **优化 SEO** - 提高搜索引擎排名
3. **添加功能** - 评论、统计、搜索等
4. **美化主题** - 让博客更漂亮
5. **绑定域名** - 使用自己的专属域名
6. **分享博客** - 让更多人看到你的内容

### 💪 持续学习

- 学习 Git 版本控制
- 了解 Markdown 语法
- 探索更多 Hexo 插件
- 研究网站性能优化

---

## 📞 需要帮助？

如果遇到问题：

1. **查看错误日志** - Cloudflare Pages 的构建日志很详细
2. **搜索问题** - 99% 的问题都有人遇到过
3. **查看官方文档** - 最权威的解决方案
4. **社区求助** - Hexo 和 Cloudflare 都有活跃的中文社区

---

## 🎊 写在最后

部署博客只是开始，重要的是坚持写作和分享。

祝你的博客越来越好！🚀

---

**本教程最后更新时间：2025年11月16日**

如有问题或建议，欢迎反馈！
